= form_for @box_type do |f|
  - if @box_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@box_type.errors.count, "error")} prohibited this box_type from being saved:"
      %ul
        - @box_type.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
