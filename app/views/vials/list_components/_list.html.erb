<%if @vials.empty? %>
<span>No vial</span>
<%else%>
	  <table class="table table-striped">
	    <thead>
	    <th class="col-xs-6"></th>
	    <th class="col-xs-6"></th>
	    </thead>
	    <tbody>
		  	<% @vials.each do |s| %>
	      		<tr class="editable" data-id = "<%= s.id %>">
	          		<%= render  partial: "vials/list_components/vial", locals: {object: s, source: source} %>
	        	</tr>
	      	<% end %>
	    </tbody>
	  </table>
<div class="row">
  <div class="col-lg-12 text-center">
    <%== pagy_bootstrap_nav(@pagy_vials) %>
    <%== pagy_bootstrap_nav_js(@pagy_vials) %>
  </div>
</div>
<%end%>