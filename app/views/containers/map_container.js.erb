$("#help-message").html("Click on a rack.");
$("#map-container").html("<%= escape_javascript(render :partial =>  "containers/components/front_view_small", :locals => {container: @container})%>");
$('[data-toggle="popover"]').popover({html:true});

console.log('New container selected');

$('#container-select').on('select2:clearing', function (e) {
  $("#help-message").html("Please, select a container.");
$("#map-container").html("");
$("#map-shelf-rack").html("");
});


$(".rack").on("click", function() {
    //Changement de couleur du fond du rack cliqué
    $(this).css('backgroundColor', "#5bc0de");
    $('.rack').not(this).each(function(){
                $(this).css('backgroundColor', "#337ab7");
             });
    //
    getShelfRackMap($(this).data("id"), '#map-shelf-rack');
    $('#map-shelf-rack').attr({'data-id': $(this).data("id")});
})

function btnColor(myclass, color) {
var property = $(myclass);
    if (property.className !== 'toggled') {
        property.css('backgroundColor', color);
        property.className = 'toggled'
    }
    else {
        property.css('backgroundColor', '');
        property.className = '';
    }
}