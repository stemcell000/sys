<small>
	  <table class="table table-striped">
	    <thead>
	    <th class="col-xs-4">Name</th>
	    <th class="col-xs-3">Box name</th>
	    <th class="col-xs-2">Box barcode</th>
	    <th class="col-xs-1">Position</th>
	    <th class="col-xs-1">Container</th>
	    <th class="col-xs-1"></th>
	    </thead>
	    <tbody>
		  	<% @vials.each do |s| %>
	      		<tr class="editable" data-id = "<%= s.id %>">
	          		<%= render  partial: "batches/components/vial", locals: {object: s} %>
	        	</tr>
	      	<% end %>
	    </tbody>
	  </table>
<div class="row">
  <div class="col-lg-12 text-center">
    <%== pagy_bootstrap_nav(@pagy) %>
    <%== pagy_bootstrap_nav_js(@pagy) %>
  </div>
</div>
</small>