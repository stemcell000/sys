	  <table class="table table-striped">
	    <thead>
	    <th class="col-xs-5">
	        Name
	    </th>
	   	<th class="col-xs-3">
	        Type
	    </th>
	   	<th class="col-xs-1">
	        Vials
	    </th>
	    <th class="col-xs-3"></th>
	    
	    </thead>
	    <tbody>
		  	<% batches.each do |s| %>
	      		<tr class="editable" data-id = "<%= s.id %>">
	          		<%= render object: s, partial: "batches/list_components/batch", locals: {object: s} %>
	        		</tr>
	      	<% end %>
	    </tbody>
	  </table>
<div class="row">
  <div class="col-lg-12 text-center">
    <%== pagy_bootstrap_nav(@pagy) %>
    <%== pagy_bootstrap_nav_js(@pagy) %>
  </div>
</div>