	  <table class="table table-striped">
	    <thead>
	    <th class="col-xs-4 text-center">
	        <%= sort_link(@q, :name, 'Name') %>
	    </th>
	   	<th class="col-xs-3 text-center">
	        <%= sort_link(@q, :batch_type_id, 'Type') %>
	    </th>
	    <th class="col-xs-1 text-center">
	        <%= sort_link(@q, :clonenb, 'Clone') %>
	    </th>
	    <th class="col-xs-1 text-center">
	        <%= sort_link(@q, :passagenb, 'Passage') %>
	    </th>
	   	<th class="col-xs-1 text-center">
	        <%= sort_link(@q, :nb, 'Vials') %>
	    </th>
	    <th class="col-xs-2 text-center"></th>
	    
	    </thead>
	    <tbody>
		  	<% batches.each do |s| %>
	      		<tr class="editable" data-id = "<%= s.id %>">
	          		<%= render object: s, partial: "batches/list_components/batch", locals: {object: s} %>
	        		</tr>
	      	<% end %>
	    </tbody>
	  </table>
<div class="row">
  <div class="col-lg-12 text-center">
    <%== pagy_bootstrap_nav(@pagy) %>
    <%== pagy_bootstrap_nav_js(@pagy) %>
  </div>
</div>