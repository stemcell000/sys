<nav class="navbar navbar-default navbar-fixed-top navbar">
    <div class="navbar-header">
    		<%=link_to(image_tag("/images/Logo-A.png", size: "80x30", alt: "SYS"), root_path, class: "navbar-brand")%>
    	</div>
      <ul class="nav navbar-nav">
          	<%= content_tag(:li, link_to( "Boxes", boxes_path, class: "nav-link"), remote: true)%>
          	<%= content_tag(:li, link_to( "Vials", sorter_vials_path, class: "nav-link"), remote: true)%>
          	<%= content_tag(:li, link_to( "Containers", containers_path, class: "nav-link"), remote: true)%>
            <%= content_tag(:li, link_to( "Users", users_path, class: "nav-link"), remote: true)%>
	</ul>
     		<span class="navbar-text">
	     		<small>
					<% if signed_in? %>
		    				<small><%=t('menu.sign_in_as')%> : <strong><%= current_user.full_name %></strong> - <%=role_set(current_user.role)%>
		    					<%=current_user.teams.nil? ? 'Team':'Team'.pluralize(current_user.teams.count)%> : <%=current_user.teams.nil? ? "-":current_user.teams.pluck(:name).to_sentence.truncate(27, separator: ' ')%>
			    			<%= link_to user_path(current_user.id), method: :get do %><i class="fa fa-user"></i><%end%>
			    			<%= link_to destroy_user_session_path, method: :get do %><i class="fa fa-sign-out-alt"></i><%end%>
		    				</small>
		    			<%end%>
	    			</small>
     		</span>
</nav>
	
